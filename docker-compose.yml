services:
  moodcreator:
    build: .
    container_name: moodcreator
    ports:
      - "4444:4444"
    volumes:
      - ./data:/app/data
    restart: unless-stopped
    environment:
      - TZ=Europe/Prague